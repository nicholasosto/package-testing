import { Value } from "@rbxts/fusion";

export const PlayerResources = {
    health: {
        current: Value(100),
        max: Value(100)
    },
    mana: {
        current: Value(50),
        max: Value(50)
    },
    stamina: {
        current: Value(75),
        max: Value(75)
    }
};
